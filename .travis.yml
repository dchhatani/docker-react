language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t dchhatani/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dchhatani/docker-react npm run test -- --coverage

